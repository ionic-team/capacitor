{
	"scripts": {
		"publish": "lerna run build && lerna run test && lerna publish --force-publish --skip-git && npm run deploy",
		"deploy": "bash scripts/deploy.sh"
	},
	"devDependencies": {
		"lerna": "^2.4.0"
	}
}
