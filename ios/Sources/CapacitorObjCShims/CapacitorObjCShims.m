// This target vends the Objective-C consumer headers that depend on the Swift `Capacitor`
// module (via `@import Capacitor;`). This translation unit exists so the target produces an
// object file for the linker; a headers-only target produces none.
#import <Foundation/Foundation.h>
